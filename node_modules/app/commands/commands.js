"use strict";

var commands = [
  require('app/commands/link_hijacker/controller'),
  require('app/commands/adventure/commander'),
  require('app/commands/snark/commander'),
  require('app/commands/terminal/exit'),
  require('app/commands/navigation/model'),
  require('app/commands/pages/ls'),
  require('app/commands/help/commander'),
  require('app/commands/link_hijacker/commander'),
  require('app/commands/external_linker/commander'),
];

module.exports = {
  register: function(){
    commands.forEach(function(cmd){
      new cmd();
    });
  }
};
