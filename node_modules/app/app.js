var $ = require('jquery');
var ArticlesController = require ('app/articles/controller');
window.Promise = require('app/utils/promise');

$(function(){
  articleController = ArticlesController.create($('.article'));
  articleController.load('dummyArticle');
});
