var commandRegistry = require('app/terminal/commands/registry');

function Commander(router){
  var routeToArticle = function(match, streamOutput) {
    var id = match[1];

    streamOutput('Routing to article ' + id);

    router.push({
      controller: 'articles',
      id: id
    });
  };

  var command = commandRegistry.register({
    matcher: /^article (.*)/,
    fn: routeToArticle,
  });
}

module.exports = Commander;
