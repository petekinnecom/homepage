"use strict";

var terminal = require('app/terminal/model');

function Commander(){
  var link = function(match){
    link = match[1];
    window.location.href = link;
  };

  terminal.registerCommand({
    matcher: /link "?([^"]*)"?/,
    fn: link,
  });

}

module.exports = Commander;
