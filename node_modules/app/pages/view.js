'use strict';
var $ = require('jquery');

function View($container, page){
  var $el = $('<div class="fade-in"></div>');
  $el.html(page.get('body'));

  $container.html($el);
}

module.exports = {
  create: function($container, page){
    new View($container, page);
  }
};
