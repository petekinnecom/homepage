var commandRegistry = require('app/terminal/command_registry/registry');
var terminal = require('app/terminal/model');

function Commander(router){
  var command, run;
  this.snark = 0;

  run = function(match){
    terminal.warn(snarks[this.snark%snarks.length]);
    this.snark += 1;
  };

  terminal.registerCommand({
    matcher: /^\s*$/,
    fn: run.bind(this),
  });
}

var snarks = [
  "Please enter a command.  Try 'help' for help.",
  "You didn't type anything...",
  "Why would you type nothing",
  "I'm afraid I can't do that, Dave",
];

module.exports = new Commander();
