var terminal = require('app/terminal/model');

function View($container){
  this.$el = $container;

  this.$el.on('keypress', function(event){
    if (event.keyCode === 13) {
      terminal.input(event.target.value);
    }
  });
}

module.exports = View;
