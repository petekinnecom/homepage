var Prompt = require('app/terminal2/input/prompt/model');
var terminal = require('app/terminal2/model');

function Controller(){
  var prompt = new Prompt();

  terminal.initialize(prompt);
  window.terminal2 = terminal;
}

module.exports = Controller;
