'use strict';
var $ = require('jquery');

function View($container, article){
  var $el = $('<div class="fade-in"></div>');
  $el.html(article.get('body'));

  $container.html($el);
}

module.exports = {
  create: function($container, article){
    new View($container, article);
  }
};
