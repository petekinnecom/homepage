var machine = require('app/articles/cache_machine/controller');

//todo: when to start
setTimeout(machine.start, 3000);

module.exports = {
  find: function(name){
    return machine.get(name); 
  },
};
