var Model = require('app/articles/model');

// temp store on DOM
var $ = require('jquery');

module.exports = {
  find: function(articleName){

    return Model.create({
      id: 'lebowski',
      body: $('#filler-text').html(),
    });

  },
};
